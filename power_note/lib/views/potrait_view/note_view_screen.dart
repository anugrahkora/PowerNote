import 'package:flutter/material.dart';

class PotraitNoteViewScreen extends StatelessWidget {
  const PotraitNoteViewScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
